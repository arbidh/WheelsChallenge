# Uncomment the next line to define a global platform for your project
platform :ios, '9.0'

target 'SOFChallenge' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  pod 'SDWebImage'
  pod 'PureLayout'
  # Pods for SOFChallenge

  target 'SOFChallengeTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'SOFChallengeUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end
